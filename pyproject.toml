[project]
name = "experiment-lab"
version = "0.1.0"
requires-python = ">=3.12,<3.13"

dependencies = [
    "numpy",
    "matplotlib",
    "pandas",
    "jupyter",
    "pytest>=9.0.3",
]

[dependency-groups]
dev = [
    "ruff"
]
